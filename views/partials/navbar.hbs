<nav class="navbar sticky-top border-bottom navbar-expand-lg navbar-dark py-3 bg-danger ">
  <div class="container text-light">

    <a class="navbar-brand" href="#">
      {{!-- <img src="{{data.brandIconUrl}}" style="border-radius: 50%;" alt="" width="35" height="35"
        class="d-inline-block align-text-top"> --}}

      {{data.brandName}}</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        {{#each data.links}}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{this.url}}">{{this.label}}</a>
        </li>
        {{/each}}
      </ul>

    </div>
  </div>
</nav>